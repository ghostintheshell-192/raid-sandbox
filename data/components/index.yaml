# index.yaml — physical component resource family
# Lists all components available in the physical layer sidebar.
# Each entry: { id, file }  — id must match the file's own `id` field.
# Only components listed here are loaded at startup.

- id: backplane
  file: backplane.yaml
- id: hba
  file: hba.yaml
- id: engine-roc
  file: engine-roc.yaml
- id: engine-metadata
  file: engine-metadata.yaml
- id: os-linux
  file: os-linux.yaml
- id: os-windows
  file: os-windows.yaml
- id: pcie
  file: pcie-bus.yaml
- id: cpu
  file: cpu.yaml
