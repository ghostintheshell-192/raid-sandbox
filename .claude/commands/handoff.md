---
description: Update session handoff notes before exiting
---

Invoke the `session-handoff` skill.

The skill owns the procedure — location, filename format, structure. Do not
restate it here: a second copy of the rules is how the two drift apart.
